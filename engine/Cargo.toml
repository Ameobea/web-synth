[workspace]
members = [
  "engine",
  "common",
  "midi",
  "polysynth",
  "spectrum_viz",
  "wavetable",
  "waveform_renderer",
  "granular",
  "event_scheduler",
  "sidechain",
  "noise_gen",
  "distortion"
]

[profile.release]
# debug-assertions = true
panic = "abort"
debug = true
