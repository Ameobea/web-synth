import React from 'react';

import './LooperUI.scss';

export interface LooperUIProps {
  vcId: string;
}

const LooperUI: React.FC<LooperUIProps> = ({ vcId }) => {
  return <div className='looper'>TODO</div>;
};

export default LooperUI;
